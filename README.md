# Brew Haven Coffee Shop

**Author:** RealitiNova
**Project Type:** Static Website (HTML/CSS)  
**Repository:** Brew-Haven-Coffe-Shop

---

## Overview / Description
Brew Haven Coffee Shop is a simple, responsive, multi-page website for a coffee shop. The site provides a clean landing page experience, a menu page for browsing items, and a contact page where visitors can find contact details and reach out.

---

## How to Use / Functionality
- **Home (`index.html`)**: Landing page with key highlights and navigation.
- **Menu (`menu.html`)**: Displays coffee shop items and menu information.
- **Contact (`contact.html`)**: Provides contact details and a contact form layout for visitor messages.
- **Navigation**: Use the menu links in the header to move between pages.

To run locally:
1. Download or clone the repository.
2. Open `index.html` in your browser.

---

## Technologies Used
- **HTML5** (`.html`)
- **CSS3** (`.css`)

---

## Project Structure
- `index.html` — Home page  
- `menu.html` — Menu page  
- `contact.html` — Contact page  
- `css/` — Stylesheets  
- `assets/` — Images and media files  
- `docs/user-stories.md` — User stories (course deliverable)  
- `wireframes/` — Wireframe images (one per view)

---

## Future Improvements (3+)
1. Add JavaScript for improved interactivity (e.g., menu filtering, animations, form validation).
2. Connect the contact form to a backend service (or email API) so submissions are actually sent/stored.
3. Improve accessibility (ARIA labels, better keyboard navigation, contrast checks).
4. Add an online ordering/reservation feature and a simple cart/checkout flow.
5. Optimize performance (image compression, lazy loading, minified CSS).

---

## Live Site (GitHub page)
After enabling GitHub Pages in repository settings, the site can be accessed publicly via GitHub Pages (URL provided by GitHub).